# use bat if available, else use cat
if command -v bat >/dev/null 2>&1; then
    alias cat='bat'
fi

# use rg if available, else use grep
if command -v rg >/dev/null 2>&1; then
    alias grep='rg'
fi

# use fd if available, else use find
if command -v fd >/dev/null 2>&1; then
    alias find='fd'
fi

# if zoxide is available, use it for directory jumping
if command -v zoxide >/dev/null 2>&1; then
    alias cd='z'
fi

# Git worktree
alias gwl="git worktree list"
alias gwa="git worktree add"
alias gwr="git worktree remove"

# General aliases
alias ll='ls -lAFh'                         # Preferred 'ls' implementatioss
alias wget='wget -c'                        # Preferred 'wget' implementation (resume download)
alias c='clear'                             # c:            Clear terminal display
alias path='echo -e ${PATH//:/\\n}'         # path:         Echo all executable Paths
alias gl='git pull'
alias gp='git push'
alias gs='git status'
alias ga='git add'
alias gc='git commit'
alias lli="ls -lh --color=always | sort -k9 -f"
alias fnv='nvim $(fzf --preview "bat --style=numbers --color=always {}" --layout reverse --border)'


# SSH Aliases
alias chip="ssh sroydip1@chip.rs.umbc.edu"
alias magpie="ssh -p 35800 -l sroydip1 magpie.cs.umbc.edu"
alias corax="ssh -p 35800 -l sroydip1 corax.cs.umbc.edu"
alias loxo="ssh -p 35800 -l sroydip1 loxo.cs.umbc.edu"
alias scify="ssh -p 35800 -l sroydip1 scify.cs.umbc.edu"


# Python virtual environment aliases
alias uvc='uv venv .venv/$(basename $(pwd))-$HOSTNAME --python 3.12 --seed'
alias uva='source .venv/$(basename $(pwd))-$HOSTNAME/bin/activate'
alias uvd="deactivate"

# Neovim nuke - delete all plugins, mason servers, caches, and state
alias nvim-nuke='rm -rf ~/.local/share/nvim/lazy ~/.local/share/nvim/mason ~/.cache/nvim ~/.local/state/nvim && echo "Nuked nvim. Next launch will reinstall everything."'

# Homebrew
alias brewdump='brew bundle dump --file=~/.config/brew/mac.Brewfile --no-vscode --force'
alias brewinstall='brew bundle --file=~/.config/brew/mac.Brewfile'

# yadm
alias yadm='yadm --yadm-archive ~/.config/yadm/archive'
